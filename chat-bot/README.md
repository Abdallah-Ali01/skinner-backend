## Setup

1. Install Python 3.10+

2. Install dependencies:
   pip install -r requirements.txt

3. Run the server:
   cd src
   uvicorn main:app --host 0.0.0.0 --port 8001 --reload

4. Test it at: http://localhost:8001/docs